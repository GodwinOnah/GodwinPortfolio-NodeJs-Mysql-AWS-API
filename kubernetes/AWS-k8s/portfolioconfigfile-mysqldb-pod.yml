apiVersion: v1
kind: Pod
metadata:
  name: portfolio-mysql-pod
  labels:
    environment: production
    app: portfolio-mysql-pod-aws
spec:
  containers:
  - name: portfolio-mysql-container
    image: daddykiki/potfoliodatabase #image from docker hub
    ports:
    - containerPort: 80